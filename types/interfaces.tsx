export interface Page {
	title: string;
	link: string;
	isActive: boolean;
}
export interface StarkPill {
	id: number;
	cost: number;
	owner: string;
}
export interface Trait {
	id: number;
	name: string;
	link: string;
}
